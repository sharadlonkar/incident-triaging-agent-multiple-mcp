spring:
  application:
    name: mcp-dynatrace-server
  ai:
    mcp:
      server:
        name: dynatrace-mcp-server
        version: 1.0.0
        type: SYNC
        sse-message-endpoint: /mcp/message

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.walmart.mcp.dynatrace: DEBUG
