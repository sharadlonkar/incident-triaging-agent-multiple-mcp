spring:
  application:
    name: mcp-openobserve-server
  ai:
    mcp:
      server:
        name: openobserve-mcp-server
        version: 1.0.0
        type: SYNC
        sse-message-endpoint: /mcp/message

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.walmart.mcp.openobserve: DEBUG
